spring:
  application:
    name: token-service
  jpa:
    show-sql: true
    open-in-view: false

  profiles:
    active: dev

server:
  servlet:
    encoding:
      charset: UTF-8
      force: true

