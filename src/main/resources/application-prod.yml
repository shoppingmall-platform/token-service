data:
  redis:
    host: ${REDIS_HOST}
    port: 6379
    password:

jwt:
  private-key: ${JWT_PRIVATE_KEY}
  public-key: ${JWT_PUBLIC_KEY}

jle:
  host: ${SERVER_HOST}
  api:
    member: ${API_MEMBER_URL}    #ci/cd 변수 추가해줘야함
