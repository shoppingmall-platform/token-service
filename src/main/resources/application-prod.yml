server:
  port: ${TOKEN_SERVICE_PORT}

spring:
  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}
      password: ${REDIS_PASSWORD}

rsa:
  private-key: ${JWT_PRIVATE_KEY}
  public-key: ${JWT_PUBLIC_KEY}

jle:
  host: ${SERVER_HOST}
  api:
    member: ${API_MEMBER_URL}
